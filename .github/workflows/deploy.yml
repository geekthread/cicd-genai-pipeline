name: "Deploy Application"
on: [push]
jobs:
  checkout:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6 
        with:
          path: myfolder
      - name: List files
        run: ls -la myfolder